# Core web app runtime
Flask>=3.0.0,<4.0.0
flask-cors>=4.0.0,<7.0.0
waitress>=3.0.0,<4.0.0
python-dotenv>=1.0.0,<2.0.0
requests>=2.31.0,<3.0.0

# Data processing and ML
# NOTE: the .joblib models were trained with numpy 2.x + scikit-learn 1.8.x.
# Keep these ranges in sync with the training environment or the pickles
# will fail to load (numpy 1.x cannot read numpy 2.x pickles).
pandas>=2.2.0,<4.0.0
numpy>=2.1.0,<3.0.0
scikit-learn>=1.8.0,<1.9.0
joblib>=1.3.0,<2.0.0
scipy>=1.11.0,<2.0.0
statsmodels>=0.14.0,<0.15.0
matplotlib>=3.7.0,<4.0.0
seaborn>=0.12.0,<1.0.0

# CV parsing
pdfplumber>=0.10.0,<0.13.0

# Scheduler
schedule>=1.2.0,<2.0.0

# Improved salary model (XGBoost)
xgboost>=2.0.0,<4.0.0

# Optional: uncomment for Google Trends support
# pytrends>=4.9.0,<5.0.0

# OCR-based CV parsing
opencv-python-headless>=4.8.0,<5.0.0
easyocr>=1.6.0,<2.0.0
Pillow>=10.0.0,<12.0.0
pytesseract>=0.3.10,<1.0.0

# Optional: uncomment for web scraping
# beautifulsoup4>=4.13.0,<5.0.0
# selenium>=4.0.0,<5.0.0
# webdriver-manager>=4.0.0,<5.0.0

# Jupyter (development only)
# jupyter>=1.0.0
# nbconvert>=7.0.0
# papermill>=2.0.0
