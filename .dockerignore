.git
.venv
__pycache__
*.pyc
*.pyo
*.pyd
.env
.dockerignore
Dockerfile
docker-compose.yml
.gitattributes
.gitignore
node_modules

# Raw Kaggle dataset (5.8GB) — never needed in the image
Dataset/
notebooks/
tests/
docs/

# NOTE: data/, models/, reports/ MUST be included — the Render image runs
# standalone (no volume mounts). docker-compose overrides them with volumes
# locally, so including them costs nothing there.
data/uploads/

# Misc
*.ipynb
*.log
.ipynb_checkpoints
